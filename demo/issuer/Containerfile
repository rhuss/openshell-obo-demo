FROM docker.io/library/node:22.22.1-slim
RUN apt-get update && apt-get install -y --no-install-recommends curl && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY token-issuer.js .
EXPOSE 8080
CMD ["node", "token-issuer.js"]
